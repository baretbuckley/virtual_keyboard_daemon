
Todo list for future additions the the current program

Search key name
- add opts for all produced exes (just need to do vkeytool opts at this point, but should need much)
- catch up windows back end to current (only vkeytool left to do)
- should you be able to do multiple keys at once using +? (i.e. press A+B+Shift)
- have client and server channel and keyboard objects be used as objects rather than passing pointer from stack?
    would need to impl struct description in header, but should be able to eliminate the last of the mallocs in the program
- investigate the emulating a windows driver instead of sendinput for windows (performance needs to be checked) 
    - If not should probably roll back the server from being needed on windows as currently it is pointless
- limit need for sudo when program fails when parsing args (this means delaying channel creation till its needed)
- Revise the daemon construction to better differentiate when it should create it's own socket or recieve from systremd
- Update readme: add compile directions

Completed tasks

-add key safety (keeps track of which keys are being held and releases all at the end of handling messages from a client)
- impliment repeat sub command. This will repeat the following command n times with optional delay
- enable keycodes to be inputed directly (this keycodes would be os specific)
    completed using "press -k <keycode number>"
- reducing mallocs, needing to malloc for serial buffers is unnecessary as the size is known compile time
- Allowing messages to be broken up if larger than buffer, this includes breaking a single type or typedelay message into 2 or more to fit
- Give ability to specify channel name, for linux an additional flag is provided to specify the complete path
- expand the press cmd usage to add flags for pressing shift, ctrl, and/or alt
- build an actual readme to reflect the current project
- make the linux backend a propper daemon 
- combine key files (use macros to select between implimentations, started in keycode_linux.h)

